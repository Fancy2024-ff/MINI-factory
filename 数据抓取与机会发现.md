# 项目诊断与修补分工方案

> 范围：基于对当前代码的实证调查（非印象），诊断「数据不刷新」「生成小程序是壳、与原 App 功能对不上」两大现象的根因，并给出离「最终成行」的系统性差距清单 + 可分配给多个 Claude 并行执行的详细分工指令。
> 口径：所有结论以当前代码为准，附 file:line 证据。

---

## 第一部分：诊断结论（实证）

### 现象 1：每次点 auto/crawl，数据永远是那几个 App

**抓取本身是真实联网的**（真调 Apple iTunes Search API + RSS 榜单、Google Play 直爬），不是 mock/样例数据。但有三层原因叠加导致「数据不刷新」：

1. **【真 Bug，当前现场原因】字符串被按字符遍历**
   网页点 crawl 时，平台参数 `"app_store"` 作为**字符串**透传到抓取循环，`for platform in platforms` 把它拆成了 `'a','p','p'...` 单字符，每个字符匹配不到任何地区 → 一次都没真抓 → 队列被清空写成 `[]`。
   - 证据：[core/pipeline/task_worker.py](core/pipeline/task_worker.py) crawl 路径未对 `platforms/regions` 做 `_csv()` 拆分（而 auto 路径做了），直接把字符串传给 `run_once`。
   - [core/opportunity/crawl_runner.py](core/opportunity/crawl_runner.py) `for platform in platforms:` 遍历字符串。
   - 实测复现：`data/opportunity/crawl-report.json` 中 `tasks: []`、起止时间差仅 9ms（根本没发包），队列文件全为 `[]`。

2. **【放大器】当天快照缓存**
   同一天重复抓取命中当日快照直接读盘、不重抓，跨天才刷新。
   - 证据：[core/opportunity/crawl_config.py](core/opportunity/crawl_config.py) `use_cache=True`；crawl_runner 命中 `snapshots/<date>/...` 即跳过请求。

3. **【放大器】已生产 App 永久隐藏 + 窄抓取过不了评分门槛**
   生成过的 App 从候选池永久差集过滤（池子越用越窄）；抓取范围窄时 confidence 不够，机会全被判 skip 不入队。
   - 证据：[apps/api/main.py](apps/api/main.py) 候选按 `parent_app_key` 差集过滤；[core/opportunity/opportunity_brief.py](core/opportunity/opportunity_brief.py) `decide_recommendation` 要求 `final_score≥65 且 confidence≥40`，confidence 主要靠多地区/多入口累加。

### 现象 2：生成的小程序是个壳，与原 App 拆分功能对不上

**这不是 Bug，是架构设计的必然**，且根子在最上游的功能拆分，不在生成器。两层归约把所有 App 碾成同一批壳：

1. **拆分阶段：关键词贴标签，非语义分析**
   功能拆分不是真分析 App，而是预定义关键词命中即贴预设标签。全项目只有 **14 个预设功能**（修图/去背景/去水印/头像/表情包/梗图/宠物说话/祝福/封面/海报/文案/翻译/摘要），且全是图片文本类。复杂 App（如 CapCut 的剪辑/特效/模板库）的独特功能在这一步直接消失或被标记「不做」。
   - 证据：[core/opportunity/feature_extraction.py](core/opportunity/feature_extraction.py) `_DOABLE_FEATURES` 表 14 项；`extract_features` 中 `if not any(kw in text for kw in kws): continue` 即关键词命中贴标签；`description` 是模板拼接 `f"从 {parent_name} 中拆出的轻量能力：{name_cn}"`。

2. **生成阶段：模板套壳，不按功能生成**
   生成器是纯模板拷贝 + 替换 6 个文案占位符，不调 LLM、不按功能生成页面逻辑。8 个模板共用同一个「输入框→出一张图」骨架，差异仅文案/配色/1-2 个静态装饰页。拆出的 `required_capabilities` 在生成器里根本没被读取（grep 零命中）。
   - 证据：[core/generator/codegen.py](core/generator/codegen.py) `shutil.copytree` 复制 base 骨架 + overlay，`text.replace(k, v)` 只替换 `__APP_NAME__` 等 6 个 token；`core/generator/` 内 grep `llm/anthropic/openai/claude` 零命中。
   - [core/generator/prd_builder.py](core/generator/prd_builder.py) PRD 页面是写死的 4 页常量（index/form/result/profile），与 App 功能无关。

3. **QA 查不出来**：QA 只验「能否构建成功」（文件存在/编码/npm build 产物），不验「功能对不对/能不能用」。
   - 证据：[core/qa/engineering_qa.py](core/qa/engineering_qa.py) 全是工程构建检查，无业务语义验证。

### 离「最终成行」的系统性差距清单（按重要性排序）

| # | 缺口 | 现状 | 严重度 |
|---|---|---|---|
| 1 | 功能拆分归约到 14 个预设 | 关键词贴标签，非语义分析 | 🔴 产物与原 App 对不上的总根因 |
| 2 | 生成是套壳，不按功能生成 | 模板拷贝+替换文案，不调 LLM | 🔴 「是个壳」的直接原因 |
| 3 | 抓取字符串 Bug + 缓存 | 网页点 crawl 抓到 0 条 | 🔴 「数据不刷新」当前原因（最易修） |
| 4 | QA 只验工程不验业务 | 只查能否构建 | 🟠 绿灯=假安全感 |
| 5 | 零真实用户/流量回流 | 埋点只落本地 | 🟠 开环生产线，无法验证机会真伪 |
| 6 | 微信上架半自动 | 能上传，提交审核+回填全人工 | 🟠 上架在 upload 后断了 |
| 7 | 无监控/告警 | 失败没人知道 | 🟡 做不到 7×24 |
| 8 | 单机 SQLite + 密钥明文 | MVP 级 | 🟡 规模化 + 安全债 |

**已做对的地方**（勿误伤）：抓取真联网、Telegram 真自动闭环、miniprogram-ci 真上传、npm build 真构建验证、密钥不下发前端。问题集中在上游归约与质量/数据闭环缺失，工程实现不糙。

## 第二部分：分工方案（5 个 Claude）

### 怎么分、为什么这么分

按「依赖关系 + 可独立性」拆成 5 个 Claude。**执行顺序有讲究**：

- **Claude A（抓取修复）** 和 **Claude E（监控/运维收口）** 完全独立，可立即并行。
- **Claude B（功能拆分智能化）** 是总根因，**必须先做**，它的输出（结构化功能规格）是 Claude C 的输入。
- **Claude C（按功能生成）** 依赖 B 的产物契约，**B 完成后再做**。
- **Claude D（业务级 QA）** 依赖 C 的产物，**C 之后做**，也可在 C 进行中先搭框架。

推荐节奏：**第 1 批并行 A + B + E** → **第 2 批 C**（接 B）→ **第 3 批 D**（接 C）。

> 每个 Claude 启动时务必遵守项目规范：先用 brainstorming 做设计、TDD 先写测试、改完跑验证命令。每个 Claude 在独立 git worktree 或独立分支工作，避免互相覆盖。

---

### Claude A：修复抓取链路，让「点一次就有新数据」

**目标**：解决「每次点 crawl/auto 数据永远不变」。这是最小、最快见效的一刀。

**背景**：网页点 crawl 时 `platforms="app_store"` 作为字符串被 `for platform in platforms` 按字符遍历，导致零抓取、队列被清空。auto 路径已用 `_csv()` 拆分，crawl 路径漏了。叠加当天缓存与已生产永久过滤。

**任务清单**：
1. 修字符串 Bug：在 [core/pipeline/task_worker.py](core/pipeline/task_worker.py) 的 crawl 执行路径，对 `platforms / regions / categories / entry_types` 像 auto 路径一样做 `_csv()` 拆分成 list 再传 `run_once`；并在 [core/opportunity/crawl_runner.py](core/opportunity/crawl_runner.py) 的 `run_once` 入口对 `str` 入参做防御性 split（双保险）。
2. 加「强制刷新」开关：让网页点 crawl 时可绕过当日快照缓存（[core/opportunity/crawl_config.py](core/opportunity/crawl_config.py) 的 `use_cache` / crawl_runner 命中逻辑），通过 API 参数（如 `force_refresh`）透传。默认仍用缓存，UI 提供「强制刷新」选项。
3. 给运营一个查看/重置 `data/opportunity/processed-apps.json` 的入口（API + 前端按钮），避免候选池被历史生产记录单向掏空。
4. 验证：写测试覆盖「字符串入参 → 正确拆成 list → 真发起抓取」「force_refresh 绕过缓存」；本机实跑一次 crawl 确认队列产出非空、且两次抓取（强制刷新）结果不同。

**交付**：点 crawl 能稳定抓到当日真实数据；强制刷新可拿到新内容；processed-apps 可重置。

**验收命令**：`.venv/bin/python -m pytest core/pipeline core/opportunity -q`；本机 `POST /api/pipeline/start {mode:crawl}` 后检查 `data/opportunity/opportunity-queue.json` 非空。

---

### Claude B：功能拆分智能化（总根因，最高优先）

**目标**：解决「拆分被归约到 14 个预设、产物与原 App 必然对不上」。这是整个项目从 demo 变真工厂的分水岭。

**背景**：当前 [core/opportunity/feature_extraction.py](core/opportunity/feature_extraction.py) 的 `_DOABLE_FEATURES` 是 14 个预设功能的关键词匹配表，App 描述命中关键词就贴预设标签，App 独特功能拆不出来。需要换成**用 LLM 对 App 真实信息做语义抽取**，动态产出功能规格。

**任务清单**：
1. 设计「功能规格」结构化契约（这是 Claude C 的输入，务必先定清楚并文档化）：每个拆出的功能应包含——功能名、功能描述、所需能力类型（如 image-edit/text-gen/multi-step 等可枚举集合）、输入形态（文本/图片/多字段）、输出形态、复杂度、是否当前可落地。
2. 新增 LLM 驱动的拆分器：调用项目已有的 LLM 接入（`core/integrations`，用最新 Claude 模型），输入 App 的真实 name/description/features/category，输出**贴合该 App 的功能列表**（而非 14 选 N）。保留规则版作为 LLM 不可用时的 fallback。
3. 兼容下游：新拆分器输出的 `selected_template` / ability / task 要能映射到现有或新增的生成能力（与 Claude C 对齐契约）。不能落地的能力明确标注，不硬塞模板。
4. 成本/稳定：LLM 调用要有超时、重试、结果校验（schema 验证），避免脏数据进队列。
5. 验证：用真实复杂 App（CapCut、Notion 等）测试，确认拆出的功能贴合 App 实际、而非千篇一律的 14 个。

**交付**：拆分器能对任意 App 产出贴合其真实功能的结构化规格，并定义好交给 Claude C 的数据契约。

**关键约束**：先用 brainstorming 设计「功能规格契约」并与 Claude C 的负责人对齐，再实现。这是 B↔C 的接口，定错全盘返工。

### Claude C：按功能真实生成小程序（接 Claude B）

**目标**：解决「生成的小程序是个壳」。让生成的产物真正实现拆出的功能，而非套同一个「输入框→出一张图」壳。

**背景**：当前 [core/generator/codegen.py](core/generator/codegen.py) 是模板拷贝 + 替换 6 个文案 token，8 个模板共用同一骨架，`required_capabilities` 没被读取。需要引入**功能驱动的页面合成**：根据 Claude B 产出的功能规格，生成贴合的页面与交互。

**任务清单**：
1. 消费 Claude B 的功能规格契约：在 [core/pipeline/runner.py](core/pipeline/runner.py) 的 code_generation 步骤，把功能规格（含 required_capabilities/输入输出形态/复杂度）完整传入生成器。当前这条数据流是断的，先打通。
2. 升级生成器：从「固定模板套壳」改为「按功能规格合成页面」。两种路线供 brainstorming 阶段权衡——(a) LLM 按功能规格生成 .vue 页面代码 + 校验可编译；(b) 大幅扩充「功能→页面组件」映射库使其远比现在 8 个壳丰富。结合「真正能用」的硬约束选型（参考已有的 docs/superpowers/specs 里能力渲染器思路）。
3. PRD 页面动态化：[core/generator/prd_builder.py](core/generator/prd_builder.py) 写死的 4 页常量改为按功能数量/类型动态推导页面清单，并让 codegen 据此真正建页。
4. 多功能/多步骤支持：当前 base 模板的「单表单→单结果」范式撑不起复杂 App，需支持多页/多功能编排。
5. 真实后端对接：生成的功能页要调真实能力（如图片处理走 image-edit），不能是 mock 占位。无对应真实能力的功能进 pending 不上线（沿用已有铁律）。
6. 验证：用 Claude B 产出的真实 App 功能规格，生成产物并人工/自动确认——产物功能与原 App 拆出的功能一致、可编译、能调通后端。

**交付**：生成的小程序能体现拆出的具体功能，不再是统一壳。

**依赖**：必须等 Claude B 的功能规格契约确定后开始。

---

### Claude D：业务级 QA（接 Claude C）

**目标**：让 QA 绿灯真正代表「这个小程序能用、是用户要的功能」，而非只代表「能构建成功」。

**背景**：当前 [core/qa/engineering_qa.py](core/qa/engineering_qa.py) 只验文件存在/编码/npm build 产物，[core/qa/growth_qa.py](core/qa/growth_qa.py) 只扫关键词。没有一项验证功能可用性，所以「产物与原 App 对不上」QA 根本发现不了。

**任务清单**：
1. 新增业务级 QA 检查（在现有工程 QA 之上叠加，不删现有）：
   - 真实调一次生成接口验证产物能出图/出文（不是看日志，是真返回结果）。
   - 页面渲染冒烟（产物页面能渲染、无报错）。
   - 核心交互可点（关键按钮/输入存在且绑定了真实逻辑，非静态装饰）。
   - 功能一致性校验：产物实现的功能与 Claude B 拆出的功能规格匹配（按契约字段比对）。
2. QA 报告分级：工程通过 / 业务通过分开标注，业务不通过的产物不进上架流程。
3. 验证：构造一个「能构建但功能是空壳」的样例，确认新 QA 能把它判为业务不通过（防止假绿灯回归）。

**交付**：QA 能拦住「能构建但功能不对/不可用」的产物。

**依赖**：依赖 Claude C 的产物形态；可在 C 进行中先搭检查框架。

---

### Claude E：监控、运维与上架闭环收口（独立可并行）

**目标**：补齐「7×24 持续运行」和「上架真闭环」的运维短板。与 A/B/C/D 无代码冲突，可立即并行。

**任务清单**：
1. worker 常驻运行落地验证：项目已有 launchd/systemd 配置（[deploy/](deploy/)），在本机正式启动并验证服务重启、异常退出后自动恢复。
2. 错误监控/告警：当前全项目无监控（grep sentry/prometheus 零命中）。接入轻量错误监控 + 任务失败告警（任务失败、worker 掉线、抓取零产出都应告警），不必重型，能通知到人即可。
3. 微信上架闭环补齐：[apps/api/main.py](apps/api/main.py) 的 wechat/upload 已能 miniprogram-ci 真上传，但提交审核+结果回填全人工。评估能否自动化 submit-review 与审核状态回填（若平台 API 允许）；不能则把人工步骤做成清晰的待办看板，别假装自动。
4. 密钥管理：当前 `.env` + 本地明文 JSON。评估上生产前的加密存储方案（不一定本轮实现，先出方案）。
5. 验证：本机重启 worker 确认自动拉起；制造一次任务失败确认告警触达。

**交付**：worker 能持续自恢复运行；失败有告警；上架人工步骤清晰可控。

---

## 执行建议

1. **第 1 批并行**：Claude A（抓取，半天见效）+ Claude B（拆分契约，最关键）+ Claude E（运维，独立）。
2. **B 的契约一确定**，立即启动 **Claude C**。
3. **C 出产物后**启动 **Claude D**。
4. 每个 Claude：独立分支/worktree、先设计后编码、TDD、改完跑验证命令、不碰他人范围。
5. **B↔C 的「功能规格契约」是全局最关键接口**，两边必须先对齐再各自实现，否则返工成本最高。

> 优先级总结：#3 抓取 Bug 立刻修（最快见效）；#1+#2（拆分智能化 + 按功能生成）是项目「成行」的核心分水岭，工程量大需专门规划；#4 业务 QA 是质量护栏；#5-#8 是规模化与运维收口。


