// 生成时由 codegen 注入：当前小程序所选用的题材模板。
// __APP_TEMPLATE__ 是共享 token 契约的一部分（core/generator/codegen.py 与
// Node page-builder.ts 都会替换它）。运行时只读此常量，不依赖打包元数据。
export const SELECTED_TEMPLATE = '__APP_TEMPLATE__'
