// 生成时由 codegen 注入：当前小程序所选用的题材模板与预览类型。
// __APP_TEMPLATE__ / __APP_PREVIEW_TYPE__ 是共享 token 契约的一部分
// （core/generator/codegen.py 与 Node page-builder.ts 都会替换它们）。
// 完整结构化蓝图见同目录 blueprint.json；运行时只读这两个轻量常量，
// 不依赖打包元数据。
export const SELECTED_TEMPLATE = '__APP_TEMPLATE__'
export const PREVIEW_TYPE = '__APP_PREVIEW_TYPE__'
